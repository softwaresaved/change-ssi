[project]
name = "change"
description = "organizational change for open science"
version = "0.3.0"
requires-python = ">=3.13"
dependencies = [
    "mccole>=3.0.0"
]

[tool.mccole]
skips = [
    "*~",
    "*.pdf",
    "docs/**",
    "uv.lock"
]
